import { ProductListViewmodel } from './product-list.viewmodel';

describe('ProductListViewmodel', () => {
  it('should create an instance', () => {
    expect(new ProductListViewmodel()).toBeTruthy();
  });
});
