import { ProductRepositoryImpl } from './product.repository.impl';

describe('ProductRepositoryImpl', () => {
  it('should create an instance', () => {
    expect(new ProductRepositoryImpl()).toBeTruthy();
  });
});
