import { CreateProductUsecase } from './create-product-usecase';

describe('CreateProductUsecase', () => {
  it('should create an instance', () => {
    expect(new CreateProductUsecase()).toBeTruthy();
  });
});
