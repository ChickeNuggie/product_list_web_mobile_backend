import { SearchProductsUseCase } from './search-products.use-case';

describe('SearchProductsUseCase', () => {
  it('should create an instance', () => {
    expect(new SearchProductsUseCase()).toBeTruthy();
  });
});
