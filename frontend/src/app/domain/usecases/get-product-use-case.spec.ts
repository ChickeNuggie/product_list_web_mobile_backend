import { GetProductUseCase } from './get-product-use-case';

describe('GetProductUseCase', () => {
  it('should create an instance', () => {
    expect(new GetProductUseCase()).toBeTruthy();
  });
});
